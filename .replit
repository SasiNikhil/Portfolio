run = "npm install && cd backend && npm install && cd ../frontend && npm install && npm run build && cd ../backend && node server.js"

[env]
MONGODB_URI = "mongodb+srv://portfolio_db:Portfolio@cluster0.yojyoso.mongodb.net/portfolio-os?appName=Cluster0"
NODE_ENV = "production"
PORT = "5000"
REACT_APP_API_URL = ""

[nix]
channel = "stable-22_11"

[[ports]]
localPort = 5000
