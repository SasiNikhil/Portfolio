run = "npm install && cd backend && npm install && cd ../frontend && npm run build && cd ../backend && node server.js"

[env]
MONGODB_URI = ""
NODE_ENV = "production"
PORT = "5000"
REACT_APP_API_URL = ""

[nix]
channel = "stable-22_11"

[[ports]]
localPort = 5000
externalPort = 80
